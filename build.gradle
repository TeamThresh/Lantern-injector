/*
apply plugin: 'hello.thinkcode.demo.plugin'

buildscript {
	repositories {
		mavenCentral()
		mavenLocal()
	}
	dependencies {
		classpath 'hello.thinkcode.gradle:demoPlugin:1.0.0-SNAPSHOT'
	}
}

repositories {
	flatDir {
        dirs 'libs'
    }
}

dependencies {
	compile project(path: ':lantern')
}
/*/
apply plugin: 'groovy'
apply plugin: 'maven'
apply plugin: 'com.novoda.bintray-release'

// 업로드용
publish {
    userOrg = 'lantern-thresh'
    groupId = 'com.lantern'
    artifactId = 'lantern-injector'
    publishVersion = '0.1.65'
    desc = 'injector for lantern-sdk'
}

// 생성되는 파일 이름
group = 'com.lantern'
version = '0.1.65'
buildscript {
    repositories {
        jcenter()

    }
    dependencies {
        classpath 'com.novoda:bintray-release:0.3.4'
    }
}



sourceCompatibility = 1.7

dependencies {
    compile gradleApi()

    testCompile 'junit:junit:4.12'
    compile group: 'org.ow2.asm', name: 'asm', version: '4.2'
    compile group: 'org.ow2.asm', name: 'asm-commons', version: '4.2'
    //compile 'com.lantern.lantern:app-debug:0.1.13@aar'
}

repositories {
    jcenter()
	flatDir {
        dirs 'libs'
    }
    mavenCentral()
    mavenLocal()
}
//*/