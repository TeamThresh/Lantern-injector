/*
apply plugin: 'hello.thinkcode.demo.plugin'

buildscript {
	repositories {
		mavenCentral()
		mavenLocal()
	}
	dependencies {
		classpath 'hello.thinkcode.gradle:demoPlugin:1.0.0-SNAPSHOT'
	}
}
/*/
apply plugin: 'groovy'
apply plugin: 'maven'
apply plugin: 'com.novoda.bintray-release'
publish {
    userOrg = 'lantern'
    groupId = 'hello.thinkcode.gradle'
    artifactId = 'lantern-injector-alpha'
    publishVersion = '0.1.17'
    desc = 'injector for lantern-sdk'
}
group = 'hello.thinkcode.gradle'
version = '1.0.0-SNAPSHOT'
buildscript {
    repositories {
        jcenter()

    }
    dependencies {
        classpath 'com.novoda:bintray-release:0.3.4'
    }
}



sourceCompatibility = 1.7

dependencies {
    compile gradleApi()

    testCompile 'junit:junit:4.12'
    compile group: 'org.ow2.asm', name: 'asm', version: '4.2'
    compile group: 'org.ow2.asm', name: 'asm-commons', version: '4.2'

}

repositories {
    jcenter()
    mavenCentral()
    mavenLocal()
}
//*/